[package]
name    = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio.workspace = true
axum            = "0.8"
reqwest         = { version = "0.13", features = ["json"] }
tower-http      = { version = "0.6", features = ["cors"] }
